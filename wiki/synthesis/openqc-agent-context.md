# OpenQC Agent Context

OpenQC consumes `pyatb-lsp-tool` and `lsp-capabilities.json` to assemble diagnostics, hover, completion, symbols, examples, next-token guidance, and repair-plan hints for `pyatb` documents.

## Traceability Sources

- Raw evidence: `raw/assets/source-provenance.json`
